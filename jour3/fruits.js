const fruits = ["pomme", "poire", "fraise", "abricot"];

function afficherFruit(fruit) {
  console.log(`J'ai mangé un.e ${fruit}`);
}

afficherFruit(fruits[0]);

// fruits.forEach();

console.log("Version itérative");
